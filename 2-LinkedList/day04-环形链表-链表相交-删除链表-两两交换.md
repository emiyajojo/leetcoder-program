# 环形链表
集合秒了
# 链表相交
无可避免地需要两条链表都遍历一遍拿到两个长度,然后根据长度差值n先让较长的链表从头遍历n个节点然后短链表也开始遍历,直到相遇

# 删除倒数第n个节点
双指针, 快的要先走n+1步,+1是为了更好地通过慢指针删除

# 两两交换
本题需要虚拟头节点, 一个暂时性节点指向交换节点对中的第一个, 一个现在节点指向目标节点对的上一个节点
记住0--1--2--3这个模型,其中0是虚拟头节点,这道题就会很简单
**最后返回值**:不要忘了是返回虚拟头节点的下一个节点
